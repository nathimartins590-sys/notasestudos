# notasestudos
meus codigos

markdown

## Configurando Git

Para utilizar o Git na minha maquina  eu preciso configura determinados comandos, sendo eles:

```bash
git config --gobal
## Configurações para GitHub
1- C: \Users\ Aluno de Tal> –version
2- >git config –gobal user.name “Aluno de tal”
3- >git config –gobal user.email “Alunodetal@gmail.com”
Códigos para SSH -GITbash
1- Is -al ~/.ssh
2- ssh-keygen -t ed25519 C- “Alunodetal@gmail.com”
3- eval “$(ssh-agent -s)”
4- ssh-add ~/.ssh/id_ed25519
copiar o código abaixo!
5- clip <~/.ssh/id_eded25519.pub

Como adicionar a chave no Github

Github -> Settings -> SSH and GPS keys -> New ssh key -> colar

Como criar um repositório
Abra o Gitbash e digite os seguintes comandos abaixo
1- $ node –version
Comandos posteriores de criar um repositório
1- cd Documents/
2- $ git clone git@github.com:nathimartins590-sys/notasestudos.git

escreva “yes” e sempre clique enter e clique com o botão direito do mouse para colar

comandos extras
1- cd notasestudos/
2- cd Doc tab
3- notasestudos/
após escrever, clique enter

Aula de Hoje
sincronização do visual studio codes

faça o login com a conta do GitHub e digite o comando na pasta changes
depois clique na caixinha dentro da pasta e de preferência escreva “1 tentativa” e clique em commit e depois sync(em hipótese nenhuma clique na setinha que está no lado esquerdo do +)
comandos
Ctrl+Shift+” é um comando para abrir um terminal e criar um terminal
Ctrl+” fecha o terminal sem causar danos
Ctrl+K Ctrl+O abre um Folder
Ctrl+O abre um File
Ctrl+D selecionar o que você marcou
segure o Alt e clique nas setas do teclado para mover para outra linha e selecione para mais de uma linha
a textura dracula muda a cor normal pro roxo ou tons de roxo pros títulos principais
```